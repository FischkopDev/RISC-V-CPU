; This file will just contain some functions
; to show, what is possible with my assembly language

use system.asm       ; This will allow to use routines from another asm 005621:                  ; This will be the starting routine
    01I x5, 00, #6   ; Write the value F2 into register x5
    01I x6, 00, #1   ; Write "           "
    10 loop            ; Jump to routine loop

loop:
    11 x5, x6          ; Check if x5 = x6
    10 exit            ; if so exit the loop
    01I x6, x6, #1     ; otherwise go through loop again
    10 loop

exit:
    NOP                 ; kinda stop ope01tion...
